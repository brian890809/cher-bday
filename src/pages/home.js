import React from 'react';
import Button from '../components/button/button.component';

const Home = () => {
    return (
        <Button variant="contained" children={this.props.children}></Button>
    )
}

export default Home